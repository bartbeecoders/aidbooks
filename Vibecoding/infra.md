Before the next phase, let's setup git and our workflow.

I have a repo setup at https://github.com/bartbeecoders/aidbooks.git
It is already initialized with a README and .gitignore.

commit phase 0.

create a script to run the backend and frontend servers. Put it in the scripts folder.
If the servers are already running, the script should stop them first.