Add the possiblity to indicate that an audiobook should be uploaded as a youtube short.
This means that the audiobook will only be a short story (max 90 seconds).
Also the artwork should be generated to fit the youtube short format.
Add it also to the topic template as a property.

If it is a youtube short, always publish it as a single video, never as a playlist.


Publishing a video on youtube should use the text/description of the audiobook as the video description in the correct language.
If we publish the dutch version, we should use the dutch description.

Add the possibility to include more illustrations to youtube shorts (like with normal audiobooks)