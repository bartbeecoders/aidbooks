Expose the whole backend as a MCP server so I can control it from AI agents.

- add a MCP server to the backend
- expose all endpoints as tools
- expose all database operations as tools
- expose all job operations as tools
- expose all file operations as tools
- expose all system operations as tools
- expose a clear instruction manual for the MCP server

Write a complete document explaining how to use the MCP server and all its tools.
from hermes-agent
from claude-code
from windsurf ai