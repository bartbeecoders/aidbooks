"""ListenAI Manim renderer.

Phase G.3 ships only the package skeleton + a smoke scene that proves
the LaTeX/Cairo/Pango toolchain is wired up. G.4 fills `templates/`
with the actual diagram primitives; G.5 adds `server.py`, the NDJSON
sidecar that the Rust publisher drives.
"""

__version__ = "0.1.0"
